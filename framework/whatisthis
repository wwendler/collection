this is supposed to be a framework for a future project, which will probably have its own repository
planning time...
server code:
    Server class: this is a tcp server. send clients map data, and the movements of the other clients
    Server factory: handles tcp connections (using netstring protocol?)
m
client code:
    Game class: 
